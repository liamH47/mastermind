import React from 'react'

const TinySquare = () => {
    return (
        <div className='single-feedback'>
            
        </div>
    )
}

export default TinySquare

/**
 * maybe time for new approach?
 * this compoonent will eventually have an <img> corresponding to whether it is correct, included,ets
 * may be easier to refactor the feedback into an array and follow exact approach from rendering guesses
 */
